"use client"; import {useActionState} from "react"; import type{ShippingAdminState}from "@/app/admin/shipping-planning/actions";
export default function ShippingAdminForm({action,fields,submitLabel}:{action:(s:ShippingAdminState,f:FormData)=>Promise<ShippingAdminState>;fields:{name:string;label:string;type?:string;required?:boolean;options?:{value:string;label:string}[];multiple?:boolean}[];submitLabel:string}){const[state,fa,p]=useActionState(action,{success:false,message:""});return <form action={fa} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="grid gap-4 md:grid-cols-2">{fields.map(x=><label key={x.name} className="block"><span className="mb-1 block text-sm font-bold">{x.label}</span>{x.options?<select name={x.name} required={x.required} multiple={x.multiple} className="w-full rounded-xl border p-3">{!x.multiple&&<option value="">Seçiniz</option>}{x.options.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select>:<input name={x.name} type={x.type??"text"} required={x.required} className="w-full rounded-xl border p-3"/>}</label>)}</div>{state.message&&<p className={`mt-4 rounded-xl p-3 font-bold ${state.success?"bg-green-50 text-green-800":"bg-red-50 text-red-800"}`}>{state.message}</p>}<button disabled={p} className="mt-5 rounded-xl bg-blue-950 px-6 py-3 font-black text-white">{p?"Kaydediliyor...":submitLabel}</button></form>}