import Link from "next/link";
import { AuthorizationService } from "@/modules/authorization/services/authorization.service";
const items=[["Cycle Count","/rf/counting","🔁"],["Planlı Sayım","/rf/inventory-counts","🔢"]];
export default async function Page(){const profile=await AuthorizationService.requireRfAccess(); const allowed=["COUNT_EXECUTE"]; if(!AuthorizationService.hasAnyPermission(profile,allowed)) return <section><Link href="/rf" className="font-bold">← Ana Menü</Link><div className="mt-4 rounded-2xl border border-red-200 bg-red-50 p-5 font-bold text-red-800">Bu işlem grubuna erişim yetkiniz bulunmuyor.</div></section>; const visible=items.filter((i)=>!i[3]||AuthorizationService.hasAnyPermission(profile,i[3] as string[])); return <section><div className="mb-4 flex items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-blue-700">RF Operasyon Merkezi</p><h1 className="mt-1 text-2xl font-black">Sayım İşlemleri</h1></div><Link href="/rf" className="rounded-xl border bg-white px-4 py-3 font-bold">← Ana Menü</Link></div><div className="grid gap-3">{visible.map(i=><Link key={i[1] as string} href={i[1] as string} className="flex min-h-24 items-center gap-4 rounded-2xl border border-blue-200 bg-white p-4 shadow-sm active:scale-[0.99]"><span className="flex h-14 w-14 items-center justify-center rounded-2xl bg-blue-950 text-2xl text-white">{i[2]}</span><h2 className="flex-1 text-lg font-black">{i[0]}</h2><span className="text-2xl font-black text-blue-900">›</span></Link>)}</div></section>}